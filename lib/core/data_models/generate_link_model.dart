// Define the GenerateLinkModel if needed.
// generae_link_model.dart

class GenerateLinkModel {
  // Define fields and methods for GenerateLinkModel
  String? link;
  String? status;

  GenerateLinkModel({this.link, this.status});

  get body => null;

  // Optionally, add methods or JSON serialization/deserialization here
}
